# Stage 6 Precheck Fix Result

- 执行依据：`docs/STAGE6_PRECHECK_KNOWLEDGE_AUDIT.md`（§12.1 P0 5 项、§12.2 P1 22 项、§12.3 P2 参考）
- 执行日期：2026-09-13
- 事实优先级：Canonical Facts → 项目原始事实 → 结构化业务数据 → 项目汇总/复盘 → 跨项目 CASE
- 改动规模：69 个文件，+721 / −629 行；未新增知识主题、未新增项目、未改 knowledge ID、未改 Manifest 架构

---

## 1. Status

```text
BLOCKED
```

P0 5 项中 3 项已修复、2 项（P0-3、P0-5）因仓库内无唯一依据而保留为人工裁定；P1 22 项全部 FIXED。
验证器全绿、审计回归 0 开放发现，但**只要存在 HUMAN_DECISION_REQUIRED，即不宣称 Stage 6 Entry READY**（任务 §九）。

---

## 2. P0 Resolution

### P0-1 DATA005 合同 `sign_date` — FIXED

- **原问题**：`PC-P001-01 / PC-P002-01 / PC-P003-01` 的 `sign_date` 分别填为 `2024-03-20 / 2024-07-22 / 2024-05-15`，即对应的采购申请日或比选定标日；`PC-P001-01` 的 "签约日" 比其供应商定标日（2024-04-02）早 13 天，逻辑不可能。
- **正确依据**（逐条回读上游）：
  - `P001_11:61-62`：比选 2024-03-28 至 2024-04-02 → **签约 2024-04-08**；`P001_13:73` 复述同一日期与合同价 41.8 万元。
  - `P002_13:72`：**"2024-07-24：采购合同签订"**（本条在仓库中唯一，可独立裁定，无需推断）；与 `P002_11:74` 定标 2024-07-22、`P002_13:72` 约定 2024-08-05 前到货自洽。
  - `P003_11:59`、`P003_13:62`：**2024-06-11 专项审批通过并完成签约**；`DATA006.committed_delivery_date = 2024-08-20` = 2024-06-11 + 10 周，反向印证签约日。
- **修改内容**：`DATA005` 三行 `sign_date` 按上游改为 `2024-04-08 / 2024-07-24 / 2024-06-11`；同三行 `start_date` 同步（该表 36 行采购合同的 `start_date` 恒等于 `sign_date`，原值沿用同一错误日期，属同一缺陷）。
- **全量同类扫描**：对 `DATA005`（48 行）+ `DATA006`（36 行）逐行回读，发现另一处同类缺陷——`DATA006.PR-P002-01.request_date = 2024-07-22` 实为定标日，上游 `P002_11:55` 明确申请日期为 **2024-06-20**（`P002_11:68-70` 审批链 06-20→06-24→06-25 一致）。已改为 2024-06-20。其余 33 行通用采购台账（PC005–PC036，项目 P004–P012）无叙述层上游可比对，未改动。
- **回归结果**：`DATA005` 三行 sign/start/end 现为 `2024-04-08→2024-06-10`、`2024-07-24→2024-09-02`、`2024-06-11→2024-09-02`，全部满足 `request ≤ sign ≤ end`；新增的采购日期链校验 PASS。

### P0-2 DATA007 入职前工时 — FIXED（39 → 317 条全量清零）

- **原问题**：约 39 条工时记录的 `work_month` 早于员工 `hire_date`。
- **全量重算**：审计口径为抽样（39 条）；全量连接 `DATA007`（1619 行）× `DATA001`（286 人）后，实际违规为 **317 行、28 名员工**（其中 292 行为跨月违规、25 行为"同月但入职日晚于月初"）。
- **真实原因判定**（任务 §四 P0-2 第 3 项要求的分类）：
  - **不是"员工 ID 错配"**：1619/1619 行均能命中 `DATA001`，且每位员工的 `work_month` 集合恰好等于其所参与项目的执行区间（`DATA004.planned_start_date` → `actual_end_date`），与其他同项目成员完全一致。
  - **不是"工时月份错误"**：改写 `work_month` 会破坏项目成员构成与其项目区间的一致性，且 `DATA008/DATA010` 的项目级口径由台账合计约束，工时行是"谁在项目上"的唯一事实源。
  - **不是"项目成员生成错误"**：违规者的同项目成员中含大量不违规者（例：P001 的 EMP0039 违规、EMP0042/0043/0044 不违规），成员集合本身自洽。
  - **判定为"员工入职日期错误"**：286 人的 `hire_date` 是 `employee_id` 的算术余数（年份由 `n mod 6` 决定、日由 `n mod 12` 决定，全部 286 人只有 27 个不同取值），与 staffing 完全未协同；仓库归档脚本 `scripts/archive/fix_stage51_data_quality.py` 本身也把 `hire_date` 当作可改字段（曾把 26 名"未来入职"员工整体前移一年），即该字段是生成残留而非业务事实。
- **修改内容**（**非"统一前移"**）：对 28 名违规员工逐个按下式重算——取"该员工自己的最早工时月"，将其 `hire_date` 按整年最小步长回退到 `≤ 该月 1 日`，保留原有月/日（即保留其入职周年语义）。回退幅度 1 年 21 人、2 年 5 人、3 年 3 人，各不相同，例如：
  - `EMP0041` 2026-06-18 → 2023-06-18（最早工时 P001 2024-02）
  - `EMP0053` 2026-06-06 → 2023-06-06（最早工时 P002 2024-03）
  - `EMP0142` 2025-11-23 → 2024-11-23（P005 项目经理，P005 实际启动 2025-03-03）
  - `EMP0143` 2025-12-24 → 2024-12-24（P006 项目经理）、`EMP0149` 2026-06-06 → 2025-06-06（P012 项目经理）
- **未改动**：`DATA007` 全 1619 行未变；`DATA001` 中 258 名不违规员工未变；三位具名项目经理（EMP0001–0003）不受影响。
- **终止日期校验**：`DATA001` / `DATA007` 均无 `termination_date`（离职）字段，字段字典亦无该字段，故 `work_month ≤ termination_date` 一项不适用，无可校验对象。
- **回归结果**：`work_month < hire_date` = **0 行**；新增"入职日期 ≤ 任何工时月份"校验 PASS。

### P0-3 P001 预备费 18 万 vs 15 万 — HUMAN_DECISION_REQUIRED（BLOCKING）

- **原问题**：`DATA009 BUD00005` 记预备费 180,000；`P001_05:65`、`P001_06:59`、`P001_19:62` 三份项目原始事实文件一致记 15 万元。
- **回读结论**：
  - `enterprise_model/CANONICAL_FACTS.yaml` 与 `P001_CANONICAL_FACTS.yaml` **只有预算总额 3,000,000，不含任何分科目金额**，无法用于裁定分科目。
  - `DATA009` P001 五行为 人员 1,800,000 / 采购 500,000 / 云与许可 300,000 / 实施与差旅 220,000 / 预备费 180,000，**合计恰为 3,000,000**，与 Canonical 预算一致。
  - 除 `DATA009` 自身外，仓库内**不存在任何 P001 分科目预算的第二来源**（全库检索"预备费"仅命中 `P001_05/06/19`、`P001_17`、`CASE001` 与事件台账 `EVT-P001-019`，均无分科目表）。
  - 故两种口径都无法在不臆造数字的前提下闭合：
    - **口径 A（预备费 = 150,000）**：与 3 份项目原始事实一致，但会使 `DATA009` P001 合计降为 2,970,000，需把 30,000 移入其他四个科目之一（人员/采购/云与许可/实施与差旅）——**仓库内无任何依据指明应移入哪一个**。
    - **口径 B（预备费 = 180,000）**：`DATA009` 合计与 Canonical 成立，但需把 3 份项目原始事实（含立项申请这一预算基准文件）的"15 万元"改写为"18 万元"，即修改上游以迁就派生台账，与本任务"禁止为让下游成立而反向篡改上游"冲突；且审计已指出 180,000 与 P001 预算差额（300 万 − 282 万 = 18 万）同值，疑似生成串值。
- **处置**：**不改动任何文件**，保留为 BLOCKING。`DATA009` 其他预算科目、P001 预算总额 3,000,000、叙述层 15 万元三处目前互不一致（合计一致、单科目冲突）。

### P0-4 FAQ005 越权造强制规则 — FIXED

- **原问题**：`FAQ005:63` 原文「依据：POL008、POL017。**涉及客户秘密或 HIGHLY_SENSITIVE 内容时必须由安全/法务确认。**」
  - 该句在 GUIDANCE 级 FAQ 中凭空设立审批关卡；`POL008:74` 只写"录音、转写和外部会议工具按数据分类、参会告知和合同限制使用"，`SOP012:76` 只写"先核验参会告知、数据分类和工具边界"，均无该审批要求。
  - 且该句引用 `POL017`，而文件 `depends_on` 仅 `[POL008, SOP012]`；同文件 `:67` 又自述"不新增……审批层级"。
- **修改内容**（采用审计最小修复 (a)，**未通过补 `depends_on` 制造合法性**）：
  「答：结论：先核验参会告知、数据分类、工具条款和保存范围。依据：POL008、SOP012。按 POL008，录音、转写和外部会议工具按数据分类、参会告知和合同限制使用；数据分类口径或合同限制不明确时返回 UNKNOWN，转责任人确认，**本问答不新增审批层级**。」
- **同文件其他强制性表述复核**：逐条比对上游后，另修正 1 处无依据的升级主体 —— `FAQ005:59`「反复逾期或 L3 风险由召集人升级」→「反复逾期或涉及 L3 风险时，按 POL008 交对应职能负责人/PMO 升级处理」（`POL008:81` 明确规定 L3 交职能负责人/PMO）。其余 `:47/:51/:55` 的"必须/只有……才能/不得"逐条有 `POL008`、`SOP012` 依据，未改动。
- **回归结果**：`FAQ005` 不再出现 `HIGHLY_SENSITIVE` 与"安全/法务确认"；新增的"GUIDANCE 分配性措辞须有已声明上游"校验 PASS。

### P0-5 P003 `PR-P003-02` 日期链 — HUMAN_DECISION_REQUIRED（BLOCKING，叙述侧已消解不可能性）

- **原问题**：规格 2024-06-05（`P003_12:47`）→ 比选启动 2024-06-14（`P003_13:68`，`P003_17` 2024-06-14 期周报印证）→ **申请日 2024-06-20**（`P003_11:53`）→ 签约 2024-06-20（`P003_13:76`）。比选早于申请 6 天、申请日等于签约日。
- **唯一性检索结论**：全库检索 `PR-P003-02`（229 个 md + 全部 csv/yaml）后，可确定的只有 **2024-06-05 ≤ request_date ≤ 2024-06-14**；`P003_CANONICAL_FACTS.yaml` 的 `procurement` 条目**不含任何日期**；事件台账 `EVT-P003-011` 的日期 2024-06-20 记录的是比选完成/签约（`P003_13:76`、`P003_12:74`）。**仓库内不存在唯一证明其确切申请日期的依据。**
- **处置（按 schema 能力分层）**：
  - **叙述侧（Markdown，可表达不确定性）**：`P003_11` 表内该行改为「2024-06-05 至 2024-06-14（申请日未单独记载，见注）」，并在表下加注：「PR-P003-02 的采购申请在需求规格提出（2024-06-05，见 P003_12）之后、三家比选启动（2024-06-14，见 P003_13）之前提交；现有材料未独立记载其确切申请日期，故按区间记录，不臆造具体日期。」→ 该文件内的日期不可能性已消除。
  - **结构化侧（`DATA006.request_date`，`date` 列，字段字典要求"按业务关系"必填，无法在列内表达区间或"未知"）**：按任务 §四 P0-5 第 3 项，**保留为 BLOCKING 人工裁定**，本轮未改动该单元格。
- **回归结果**：叙述侧 PASS；台账侧与叙述侧当前不一致（台账 2024-06-20 vs 叙述区间），已在下方人工裁定清单中列明。

---

## 3. P1 Resolution

按审计 §12.2 分组汇总：**22/22 FIXED**（逐条）

| # | 审计项 | 状态 | 修改内容与依据 |
| --- | --- | --- | --- |
| 6 | CASE001 变更事实链 | FIXED | 提出日 `2024-06-18`→**2024-06-12**（`P001_19:56`）；顺序倒置改为"06-17 评审会审议、06-18 CCB 批准"；"变更全程 **11 天**闭环"→"从提出（06-12）到关闭（07-22）历时约 **40 天（约 5 周）**"（`P001_19:84`）。行首 `SOP009 项目变更管理流程`→`SOP009 项目变更控制流程` |
| 7 | CASE004 AI-Q 编号描述错配 | FIXED | 依 `P001_32:54-65` 原文重建：AI-Q01 资格册目录与附件页码索引缺失（完整性）、AI-Q05 人员证书扫描件分辨率不足（完整性）、AI-Q06 投标函签署日期留空（完整性）、AI-Q10 目录页码与技术册重排后不一致（完整性）、AI-Q12 电子标书文件名不合规（合规性）；原"格式与排版类占 5 项"（`P001_32` 无此分类）删除，改为"与文档格式与材料规范（SOP022 检查口径）直接相关的 5 项"+ 真实类型，并补"12 项中完整性 4 / 一致性 6 / 合规性 2" |
| 8 | CASE004 人工复核日期 | FIXED | `2023-12-20`→**2023-12-22**（`P001_33:50`、`EVT-P001-004`） |
| 9 | CASE002 成本预警分解 | FIXED | 补入"其他零星偏差 **−12 万元**（差旅与外采节余冲抵）"与"延期人力约 8 万元"，使 18+21+8−12 = **35 万元**闭合（`P002_29:68-75`） |
| 10 | CASE002 84 万试运行款 | FIXED | 改为"一度被暂缓（**2025-02-21 补充确认证据口径后解除暂缓**，进入支付流程），28 万元尾款挂起"（`P002_30:62`、`P002_21:74`） |
| 11 | SOP 名称漂移 9 处 | FIXED | CASE001 `:58/:88/:102` → `SOP009 项目变更控制流程`；CASE002 `:56/:81/:97` → `SOP016 采购申请与询价流程`、`SOP018 合同审核与履约跟踪流程`；CASE003 `:94/:109` → `SOP010 项目验收流程`。已由新增 ID-title 校验全库复核：**0 漂移** |
| 12 | P001 需求优先级与虚增项 | FIXED | `P001_02:99` 高 9/中 6/低 3 → **高 10 / 中 6 / 低 2**；`P001_03:66` "9 项"→**10 项**（原行标 9 却列 10 个 ID）；`:68` 删除虚增的第 19 项需求（"另含一项展示样式偏好类需求"），低项改为 2 项。依据 `P001_02:64-88` 需求表逐行计数 |
| 13 | P001_32 问题类型计数 | FIXED | "完整性 5 / 一致性 5 / 合规性 2" → **完整性 4 / 一致性 6 / 合规性 2**（=12），依同文件表的"问题类型"列逐行计数 |
| 14 | P003 未达标场景数 | FIXED | `P003_31:88` "3 个未达标场景"→**4 个**（场景 2/3/5/6，与同文件 §三表逐行判定一致）；`P003_21:59`、`P003_17:90` 同步为 4 个并列出场景号 |
| 15 | P003 样本库达标声明 | FIXED | 原"满足验收方案第二版各场景样本量要求"（实际 雨雾 2400<2500、红外 1900<2000、线路 1700<2000，三项不满足）→ 改为按 `P003_20` 阈值逐项对照：日间/夜间/表计达标，雨雾/红外/线路三项低于要求，"以双方共同抽样封样后的实测结果为准" |
| 16 | P003 试运行款触发自洽 | FIXED | `P003_16:74` "试运行款以 2024-12-20 试运行启动**满一个月**为触发"（= 2025-01-20，晚于到账 2025-01-10）→ 改为"以 **2024-12-20 试运行启动**为触发"，保留既有的到账日 2025-01-10（不臆造新日期） |
| 17 | GPU 延期口径 | FIXED | 承诺 10 周 → **2024-08-20**，实际 **2024-09-02 = 晚 13 天（约 2 周）**（与 `P003_07:74`"预留 2 周缓冲被完全消耗"自洽）。6 处正文 + 事件台账同步：`P003_07:83`、`P003_11:61`、`P003_18:71`、`P003_22:73`、`P003_25:81`、`CASE002:75`、`PROJECT_EVENT_LEDGER EVT-P003-015.impact_schedule` |
| 18 | 人力成本口径 P001/P003 | FIXED | 见下方"口径裁定" |
| 19 | P003 回款日期语义 | FIXED | 见下方"口径裁定" |
| 20 | 客户主数据状态 | FIXED | `DATA002` `CUST001`（P001 已完成 + 360 万全额回款）、`CUST008`（P008 在执行）`潜在`→**`合作中`**；`CUST015`/`CUST022` 经复核**维持 `潜在`**（`DATA004` 中无任何项目，无合作记录；审计的同类怀疑不成立） |
| 21 | FAQ001 请假审批层级 | FIXED | `FAQ001:49` "最终权限以 POL001 的现行授权为准"→"**当前 POL001 未规定具体请假审批层级，审批层级应依据实际授权与组织流程执行**，本问答不另设层级"。未创造任何审批层级（`POL001:74` 仅涉及"请假审批"口径，未定义层级） |
| 22 | 模板零信息填充 | FIXED | 见下方"TPL 处理" |
| 23 | 非 Canonical 采购阈值 | FIXED | `SRC-MOF-001.md` L58/L60、`SRC-LAW-005.md` L60/L63、`SOURCE_LINEAGE.csv` LIN-026 共 5 处（审计计 3 处，全量扫描多发现 2 处）统一到 Canonical 四档（`<5 万` 简化采购 / `5–20 万` 三家比选 / `20–100 万` 竞争性采购 / `≥100 万` 公开招标或专项审批）。**判定为 A 类（星澜内部 synthetic rule）**：这些句子原文即以"星澜自己的……内部阈值规则"起头并被标注需显式标 SYNTHETIC，故属内部规则统一，非改写外部法律；同时保留 `SRC-MOF-001`/`SRC-LAW-005` 作为外部依据的原文边界表述（"不能伪装成国家要求"） |
| 24 | CLAIM_PROVENANCE `section`/`locator` | FIXED | `section` 列 **96 行**修正为对应文件的**逐字二级标题**：`标准步骤`→`可执行步骤`（30）、`核心规则`→`3. 领域规则`（POL011×4、POL018×1）/`可引用的核心结论`（REF×13）、`快速问答`→`高频问答`（12）、`职责分工`→`2. 职责分工`（18）、`领域规则`→`3. 领域规则`（18）；`evidence_locator` 中"上游制度与本流程标准步骤"→"…可执行步骤"（30 行）。审计计 60 行，全量扫描发现后两组 36 行同样不是逐字标题（仅差 `2. `/`3. ` 编号前缀），一并修正使 97 行全部可解析 |
| 25 | P002 周报早于事件 | FIXED | `P002_17:71` 由"2024-07-19（第 19 期）…测试环境资源三家比选定标 18 万元"拆为：第 19 期只保留 R01 升级（`EVT-P002-014`）；新增「2024-07-26（第 20 期）：测试环境资源三家比选于 7-22 定标，成交价 18 万元，合同约定 8-05 前到货。`EVT-P002-015`」，与 `P002_13:71`、台账 `EVT-P002-015 = 2024-07-22` 一致 |
| 26 | P002 三月"已提招聘需求" | FIXED | `P002_17:55`、`P002_18:55` 改为"登记人员缺口，招聘需求于 **2024-06-14** 提交（见 P002_10）"，与 `P002_10:47,54` 提出日期一致；`EVT-P002-012` 亦为 2024-06-14 |
| 27 | P002 成本口径 218 vs 190 | FIXED | `P002_05:68` "计划人力与交付成本约 218 万元…对应预算 220 万元"→与立项 `P002_06:70` 一致："人力成本约 190 万元、测试环境资源 18 万元、差旅与外采约 12 万元，合计对应立项预算 220 万元"；`P002_23` 分解表基准改为 **220**，并补入 P002_29 已记载的"其他零星偏差冲抵 −12"与"CH04 样本扩大 +10"，使 **220+18+12+9+8−12+10 = 265**、超支 **45** 闭合（原表 218+18+12+9+8 = 265 虽自洽，但 218 与预算 220 无法对应） |

### 口径裁定（审计项 18、19，任务 §五 C）

两项均**不允许"选一个数字覆盖另一个"**，故先定义字段语义再比数值：

- **人力成本（P001_23 / P003_23 ↔ DATA008）**
  - `DATA008` 是成本科目 SSOT：P001 实际 282 万 = 人员成本 168 + 设备采购 42 + 云服务与许可 26 + 外包实施 18 + 差旅 9 + 项目间接费 15 + 变更执行 3.2 + 其他 0.8；P003 实际 486 万 = 人员成本 205 + GPU 采购 128 + 边缘设备采购 76 + 云 GPU 过渡 9.6 + 数据治理 24 + 现场部署 21 + 差旅 8 + 项目间接费 14.4。
  - **P001_23**：原文"人力成本 226 万元，设备采购 41.8 万元及安装辅料，其他费用约 14.2 万元"——226 无法由 `DATA008` 任何科目组合复现（最接近为 人员+云+外包+间接 = 227），属无定义口径，且 P001_23 位于"项目汇总/复盘"层、低于"结构化业务数据"层。**按 `DATA008` 科目口径改写**为该台账的八项明细（合计 282 万元）。
  - **P003_23**：原文"人力成本 264 万元"**可由 `DATA008` 复现**——人员 205 + 数据治理 24 + 现场部署 21 + 项目间接费 14.4 = 264.4 ≈ 264；"其他直接费用 18"= 云 GPU 过渡 9.6 + 差旅 8 = 17.6 ≈ 18；采购 204 = 128+76。且 268/204/18 的预算口径在 `P003_22:57`、`P003_11:73` 一致。故**保留数字**，仅在表下**显式定义科目口径**并列出 `DATA008` 细科目与 `DATA009` 五类预算的对应关系，使两套口径可核对。
- **回款日期（P003_23:54 / P003_16 ↔ DATA010）**
  - 语义判定：`DATA010` 的字段是 `due_date`（到期日）+ `recognized_date`（收入确认日），**不存在 `collected_date`（实际到账日）**；其日期与项目里程碑触发日对齐（P003 设备款 2024-09-02 = GPU 到货日；P002 试运行款 2025-01-12 = 试运行启动日），而叙述层的"到账日期"是资金实际入账口径（P001_16 的上线款 2024-10-12 = 上线确认日 2024-09-27 + 合同约定 15 日，可自证两套口径）。
  - 判定：**叙述层 2024-04-22 / 2024-10-15 / 2025-01-10 / 2025-06-27 与 `DATA010` 不构成数值冲突**，冲突来源是语义未声明。故**不改 `DATA010`、不改到账日**，改为在 `P003_16:74` 后与 `P003_23:54` 内显式声明两套口径不同、不应逐笔等同比较。
  - **未新增 `collected_date` 字段**：新增字段需同步字段字典/workbook schema/validator/全部引用，且 P004–P012 共 30 行收入无任何叙述层来源，必须臆造日期——按任务 §五 C 判定为超出最小改动范围，不新增；`P002` 试运行款"到账早于付款申请"一项同样由该语义声明消解（`recognized_date = 2025-01-12` 是收入确认日，非到账日）。

### TPL 处理（审计项 22，任务 §五 E）

- 消除全部 **942 个零信息单元格**：`完成流程和形成有效记录所需` 387 处 → `—`（字段说明列）；`业务值或关联记录` 471 处 → `—`（记录值列）；`按场景补充，不影响无关场景` 84 处 → 按字段名给出**逐字段真实使用条件**（如 `关联项目`→"涉及项目时填写"、`退出与数据返还方案`→"涉及退出与数据返还时填写"），84 条互不相同。
- **未改**：表格字段、表头、列数、状态机、模板结构、章节骨架，一律未动。
- 选择说明：字段说明列取 `—` 而非为 387 个字段各写一句新描述——该列的字段名本身即为说明（如"立项申请编号""变更影响""验收标准"），另行生成 387 条新语义串属在清理轮次内引入不可上游校验的内容，风险高于收益；审计 §12.2 第 22 项亦明确允许"统一简化为 —"。该取舍记入下方 Remaining P2 供后续阶段选择。

---

## 4. Validator Hardening

本轮新增/修改的规则（不含架构变更，不涉及 NLP 语义推断）：

| 规则 | 位置 | 级别 | 本轮可复现的缺陷 |
| --- | --- | --- | --- |
| 入职日期 ≤ 任何工时月份（`DATA001 × DATA007` 连接校验；无离职字段故不校验终止日） | `validate_stage5_assets.py` `validate_realism()` | FAIL | P0-2 |
| 采购日期链：`request ≤ committed_delivery`、`request ≤ delivery`、`request ≤ 关联合同 sign`、`sign ≤ end`（字段存在时才检查） | `validate_stage5_assets.py` `validate_realism()` | FAIL | P0-1（`request > sign` 型）、同类台账扩散 |
| 模板零信息填充守卫：三个占位串不得再出现 | `validate_stage5_assets.py` `validate_templates()` | FAIL | P1-15 / §12.2-22 防回归 |
| 模板深度改为结构化度量（必填 ≥10、可选 ≥3、填写规则 ≥5 条、检查清单 ≥5 项）。**原 `len(body) < 2200` 的字节数代理已移除**——该阈值此前主要由占位填充撑起，占位填充按审计要求删除后该代理不再成立 | 同上 | FAIL | — |
| ID-title 漂移：正文以 `ID《名称》`/`ID 名称` 引用时，名称须与 Manifest canonical title 前缀一致；仅在"名称与正式名共享 ≥3 字首且分歧"时判定，避免散文误报 | `validate_stage6_precheck.py` | FAIL | P1-2（4 组 9 处） |
| 编号族（AI-Q / REQ / JS / SW / PF / ZG）：须存在定义表行；若引用处带类型词（完整性/一致性/合规性）则须与定义表一致 | `validate_stage6_precheck.py` | FAIL | P1-3（CASE004 AI-Q 错配） |
| 表格计数闭合：`N 项` 单元格须等于同行列举项数 | `validate_stage6_precheck.py` | FAIL | P1-5（"9 项"却列 10 个）、P1-6 同类 |
| GUIDANCE/FAQ 分配性措辞（必须由 / 必须经 / 须经 / 严禁 / 只有……才能）所在行须引用其已声明依赖内的资产 | `validate_stage6_precheck.py` | REVIEW | P0-4 |
| `P00X_NN` 文档引用须命中 Manifest | `validate_stage4_cases.py` G5 | FAIL | 派生层的悬空文档引用 |
| 派生 CASE 中"P00X 延期 N 天 / 超支或节约 N 万元"须与该项目 Canonical 一致（按最近前置项目 ID 归属，排除"计划/基线/预计/预测"语境） | `validate_stage4_cases.py` G6 | FAIL | P1-1/P1-2 类的 CASE 结论漂移 |
| 注册新校验器 | `validate_all.py` | — | — |

**派生 CASE 事实校验盲点（审计 §11.2）的处置说明**：
审计指出的根因是 `validate_stage4_cases.py:123` 的 `name.startswith(f"{pid}_")` 使 `CASE001–004` 完全退出事实校验。本轮**未采用"把 CASE 正文并入各项目语料"的写法**——因为 CASE 文件是跨项目派生，并入后会被同一段代码的"串项目检测"（`corpus contains {other} contract amount`）误判为污染。改为新增**针对派生层正向可校验维度**的两条规则（G5 文档引用解析、G6 项目结论数值对 Canonical），并在下方明确声明未覆盖范围。

**明确未覆盖（不做 NLP 推断）**：
- CASE 中 `EVT-P00X-NNN` 旁的日期与实际事件日的自动比对——实测在周报摘要、里程碑表等句式下误报率高（如 P002_17 的各期周报日期为出报日而非事件日），已试做并放弃，不作为 gate。
- AI-Q/REQ 编号后**问题描述文本**的语义一致性（CASE004 的原始错配属此类）——只能靠显式类型词比对（已实现）与人工复核，未构建描述级比对。
- 叙述层"申请 ≤ 比选 ≤ 签约 ≤ 到货"链（审计 §12.1 建议）——比选日期仅存在于叙述文本，本轮未做跨文件叙述解析；结构化侧已覆盖 `request ≤ sign`。

---

## 5. Regression

| 项目 | 命令 | 结果 |
| --- | --- | --- |
| 全量校验 | `python scripts/validate_all.py` | **PASS**（Overall: PASS，13 个 validator 全 PASS） |
| 定向校验（被修改） | `validate_stage4_cases.py` / `validate_stage5_assets.py` / `validate_stage6_precheck.py` | **PASS**（三者单独执行均 PASS） |
| 空白/冲突标记 | `git diff --check` | **PASS**（退出码 0） |
| 内容审计回归 | `D:\tmp\kbaudit2\reaudit.py`（等价复跑审计 §3/§4/§7/§8/§9/§11 的机器可判部分） | **PASS**（开放发现 0） |

审计回归逐项对照（任务 §十要求）：

| 断言 | 结果 |
| --- | --- |
| P0 = 0 | **未达成**：3/5 已修，P0-3、P0-5 保留为人工裁定 |
| P1 = 0 | **达成**（22/22） |
| 跨项目污染 = 0 | 达成（PM 串项目 0、合同额串项目 0、跨项目 `EVT-` 引用 0） |
| Canonical 冲突 = 0 | 达成（P001/P002/P003 合同额/预算/实际成本/预算合计逐项一致） |
| event ledger 悬空/孤儿 = 0 | 达成（84 条事件全部有正文引用，正文引用的每个 `EVT-*` 均存在） |
| ID-title drift = 0 | 达成（全库 424 处显式引用比对，0 漂移） |
| 入职前工时 = 0 | 达成（`work_month < hire_date` 0 行） |
| 采购不可能日期链 = 0 | 达成（`request ≤ sign ≤ end` 全表成立） |
| CASE 已知漂移 = 0 | 达成（CASE001–004 已知 7 处全部消除） |
| FAQ 无依据强制规则 = 0 | 达成（`必须由/必须经/须经/严禁/只有…才能` 全库 0 处无依据） |

---

## 6. Remaining P2

以下为审计 §12.3 中本轮**未处理**的项，均不影响 Stage 6 进入条件，可在 Stage 6 内处理：

| # | 内容 | 说明 |
| --- | --- | --- |
| 29 | `P001_12` "60 个自然日" | **本轮已顺手修复**（补"较约定到货期限晚 3 天，未影响关键路径"） |
| 30/31 | P001/P002 周报期号与周五编制规则错位 | 未处理。属期号—日期—事件三者关系，逐期重排会改动大量周报摘录行；本轮只修了会导致"周报早于事实"的 P1-20 |
| 32 | `P002_13` 供应商丙"竞态方案"及"最低价未自动中选" | **本轮已顺手修复**（改为"存储项满足核心条款"、"最低价不作为唯一中选依据"） |
| 33 | 30 份 SOP 的两处自指措辞（60 处） | 未处理。属文体问题，30 文件批量改写超出"最小化修复"范围，审计亦列为 P2 |
| 34 | 采购阈值 20 万元端点开闭重叠 | 未处理。属 **Canonical 层口径**（`CANONICAL_FACTS.yaml` 的 `20万~100万` 与 `5万~20万` 同时含 20 万），审计明确"本轮不改" |
| 35 | `knowledge/README.md` 内容过期 | **本轮已顺手修复**（改为 Stage 3~5 已完成的实际目录规模） |
| 36 | `ROLE005:88` 培训方向引用四档阈值但上游为 KB002/KB003 | 未处理。修法需同时改 frontmatter 与 Manifest 血缘，属 P2-37 同类，一并延后 |
| 37 | 15 份 SOP/FAQ 的 `depends_on` 血缘声明不全 | 未处理。已实测：若按"分配性措辞须有已声明上游"的**宽口径**（含 必须/不得）启用，会命中 5 处既有血缘缺口（FAQ006→SOP013/SOP024、FAQ007→SOP016、FAQ010→POL007），使 `validate_all` 停在 REVIEW；故本轮采用**窄口径**（必须由/必须经/须经/严禁/只有…才能）作为 gate，宽口径留待补齐 15 份血缘后启用。审计已核验这 5 处引用内容确实存在于被引资产，属声明不全而非造规则 |
| 38 | 叙述层供应商名无法解析到 `DATA003`（全为"合成供应商NN"） | 未处理 |
| 39 | `SRC-LAW-002.md` 网络安全法条款号（第二十一条 vs 第二十三条） | 未处理。审计已确认"知识侧正确、来源笔记未同步"，且需按 `docs/QUALITY_GATE.md` 把该事项登记到活动路径（当前仅存于 `docs/archive/`），属流程性事项 |
| 40 | `DATA006.PR-P001-01.amount = 420,000` vs 实际成交 418,000 | 未处理（保留为"预算额"语义，需加字段说明） |
| 41 | `SOURCE_LINEAGE.csv` 陈旧行 | **部分已修**：LIN-002（与 LIN-007 重复的 PENDING 存根）已删除；LIN-004/005/006 已由 `Stage 4 生成正文`+PENDING 更新为 `derived_from_canonical_facts`/HIGH/VERIFIED + Stage 4.1 补登说明；LIN-044 `深交所`→`上交所`（并同步 `SRC-CORP-001.md` 同一错误的"发布机构"行）。**未修**：LIN-056/057 缺号——补齐需重编号 LIN-058~096 共 39 行且无检索收益，保留并在本轮记录 |
| 42 | KB 层 39.7% 逐字共享样板 | 未处理（Stage 6 采用整文件入块） |
| 43 | 通用标题不自解释（`## 关联` ×99 等） | 未处理（Stage 6 切块策略） |

另记录两条审计未列出、本轮发现的遗留项：

- **采购台账通用行（`PC005–PC036` / `PR005–PR036`）的 `sign_date ≡ request_date ≡ start_date`**：同一日期被复制到三个列，与 P0-1 属同一生成缺陷族，但 P004–P012 无任何叙述层上游可比对，按"不臆造"原则未改动；已由新增的日期链校验（`request ≤ sign ≤ end`）持续看守。
- **TPL 字段说明列采用 `—`**：如后续阶段希望为 387 个必填字段各写一条真实短说明，可在 Stage 6 或之后单独处理（不影响检索，属可选增益）。

---

## 7. Stage 6 Entry

```text
BLOCKED
```

判定条件对照：

| 条件 | 结果 |
| --- | --- |
| P0 = 0 | ✗（3/5，P0-3、P0-5 待人工裁定） |
| P1 = 0 | ✓（22/22） |
| validators PASS | ✓（`validate_all.py` Overall PASS，13/13） |
| audit regression PASS | ✓（开放发现 0） |
| 无 HUMAN_DECISION_REQUIRED | ✗（2 项） |

**需人工裁定的事项（本轮未自行处置）**

1. **P001 预备费口径：150,000 还是 180,000**
   - 证据 A（支持 150,000）：`P001_05:65`、`P001_06:59`（立项申请）、`P001_19:62` 三份项目原始事实文件一致记"预备费 15 万元"，层级高于结构化业务数据。
   - 证据 B（支持 180,000）：`DATA009 BUD00005` 记 180,000，且该行使 P001 五类预算合计恰为 Canonical 的 3,000,000。
   - 阻塞点：选 A 需把 30,000 移入其他四个科目之一，仓库内**无任何依据**指明移入哪一个；选 B 则需改写 3 份上游项目原始事实。请选择其一，或指定 30,000 的归属科目。

2. **P003 `PR-P003-02` 的确切采购申请日期**
   - 已确定：规格提出 `2024-06-05`（`P003_12:47`）≤ 申请日 ≤ 比选启动 `2024-06-14`（`P003_13:68`，`P003_17` 第 2024-06-14 期周报印证）。
   - 已处置：`P003_11` 表内已按区间记录并加注，叙述层不再出现"比选早于申请"的不可能顺序。
   - 阻塞点：`DATA006.PR-P003-02.request_date` 为 `date` 列且字段字典要求"按业务关系"必填，**无法在列内表达"区间/未知"**；当前仍为错误的 2024-06-20。请指定确切日期，或决定以空值表达未知（会违反该字段的必填声明）。

在上述两项裁定完成前，不建议进入 Stage 6。
